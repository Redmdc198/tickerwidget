const{send,fetchJson,meta,cleanSymbol,envFirst}=require("./_common");const BASE="https://finnhub.io/api/v1";
module.exports=async function handler(req,res){if(req.method!=="GET")return send(res,405,{error:"METHOD_NOT_ALLOWED"});const token=envFirst("FINNHUB_API_KEY","FINNHUB_API","FINNHUB_TOKEN","FINHUB_API_KEY");if(!token)return send(res,503,{data:null,meta:meta("Finnhub","NOT_CONFIGURED")});const symbol=cleanSymbol(req.query.symbol);if(!symbol)return send(res,400,{error:"INVALID_SYMBOL"});const action=String(req.query.action||"quote").toLowerCase();let path;if(action==="quote")path=`/quote?symbol=${encodeURIComponent(symbol)}`;else if(action==="profile")path=`/stock/profile2?symbol=${encodeURIComponent(symbol)}`;else if(action==="metric")path=`/stock/metric?symbol=${encodeURIComponent(symbol)}&metric=all`;else return send(res,400,{error:"UNSUPPORTED_ACTION",supported:["quote","profile","metric"]});const sourceUrl=`${BASE}${path}`;let result=await fetchJson(sourceUrl,{headers:{"X-Finnhub-Token":token}});let auth_mode="header";if(result.status===401){const join=sourceUrl.includes("?")?"&":"?";result=await fetchJson(`${sourceUrl}${join}token=${encodeURIComponent(token)}`);auth_mode="query_fallback"}if(!result.ok)return send(res,result.status===403?403:502,{data:null,meta:meta("Finnhub",result.status===403?"NOT_ENTITLED":"SOURCE_ERROR",{source_url:sourceUrl,elapsed_ms:result.elapsed_ms}),upstream_status:result.status,auth_mode});let observedAt=null;if(action==="quote"&&Number(result.data?.t))observedAt=new Date(Number(result.data.t)*1000).toISOString();return send(res,200,{symbol,action,data:result.data,meta:meta("Finnhub","VALID",{source_url:sourceUrl,observed_at:observedAt,elapsed_ms:result.elapsed_ms,delay_state:"UNKNOWN"}),auth_mode})};